#!/bin/sh

# Initialize default configuration
# This ensures 'snap get hermes-agent' works immediately after installation
snapctl set agent-service="disabled"
snapctl set acp-service="disabled"
snapctl set dashboard-service="disabled"
snapctl set model="gpt-4o"
snapctl set log-level="info"

# CT integration defaults (empty until configured by Control Tower)
snapctl set ct-callback-url=""
snapctl set ct-snap-name=""
snapctl set ct-node-id=""
snapctl set ct-deployment-id=""

echo "hermes-agent installed successfully"

# Inform the user about the dot-hermes interface if they want to share data with the host
echo "Hermes Agent installed. To allow access to your home directory and ~/.hermes, run:"
echo "  snap connect hermes-agent:home"
echo "  snap connect hermes-agent:dot-hermes"
echo "  snap connect hermes-agent:ssh-keys"
echo "  snap connect hermes-agent:removable-media"
